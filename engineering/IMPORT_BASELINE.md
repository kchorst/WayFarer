# Initial GitHub Import Baseline

Imported 2026-09-25 from the latest available WAYFINDER development source in the ChatGPT recovery workflow (`WAYFINDER-2026-09-25-SPARK-CONTINUATION-FIX.zip`).

This import is intentionally **UNVERIFIED** and must not be treated as a release. `RELEASE.json` remains `unverified`.

The initial import preserves the current application and tests while adding persistent GitHub CI/release enforcement. Historical traveler failures remain open regression obligations until the new production-parity/global gates prove them closed.

Local pre-import source checks executed while preparing this repository:
- Node test suite: 153/153 PASS
- syntax: PASS
- runtime smoke: PASS
- hostile/property audit: PASS (416 generated semantic mutations)
- staged-package integrity mechanism: PASS on a generated staging copy

These checks establish only that the repository was transferred coherently. They do **not** promote the application to READY.
